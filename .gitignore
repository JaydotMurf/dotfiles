.DS_Store
.zsh_history
.zsh_sessions/
.python_history
.netrc
.claude/settings.local.json
zsh/.zshrc.mac-backup

# Machine-local work identity — never tracked (contains employer email)
git/.gitconfig-work
